These scripts assume you are working in a directory with the following subfolders:

- /raw_data (provided on OSF LINK)
- /clean_data (populated by "clean_data.R"
- /plots (populated by "*_analysis.R"

The file "utils.R" must also be in the working/project directory.